# Project guide

- Use `pnpm` for dependency and script commands.
- Keep the guide concise and product-agnostic.
- Prefer plain Markdown and Rspress built-ins over custom runtime components.
- Run `pnpm run build` before deployment.
- Rspress documentation: https://rspress.rs/zh/
